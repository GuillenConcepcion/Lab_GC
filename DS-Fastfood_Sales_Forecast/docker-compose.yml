version: '3.8'

services:
  fastfood-dashboard:
    build: .
    container_name: fastfood_sales_app
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    environment:
      - STREAMLIT_SERVER_PORT=8501
    restart: unless-stopped
